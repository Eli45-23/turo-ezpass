{
  "name": "trips-api-lambda",
  "version": "1.0.0",
  "description": "Lambda function for Turo-E-Pass trips REST API",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && cd dist && zip -r ../function.zip . ../node_modules/",
    "clean": "rm -rf dist/ function.zip"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.848.0",
    "@aws-sdk/lib-dynamodb": "^3.848.0",
    "@aws-sdk/client-eventbridge": "^3.848.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}