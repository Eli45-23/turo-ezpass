{
  "name": "turo-ezpass-scrapers",
  "version": "1.0.0",
  "description": "Playwright-based scrapers for E-ZPass and Turo data extraction with automated CI/CD",
  "main": "match.js",
  "scripts": {
    "scrape-ezpass": "node scrapers/ezpass.js",
    "scrape-turo": "node scrapers/turo.js",
    "match": "node match.js",
    "submit": "node turobot.js",
    "scrape-all": "npm run scrape-ezpass && npm run scrape-turo && npm run match",
    "full-automation": "npm run scrape-all && npm run submit",
    "install-browsers": "npx playwright install",
    "test": "echo \"No tests configured yet\" && exit 0"
  },
  "dependencies": {
    "aws-sdk": "^2.1500.0",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "@types/node": "^20.0.0"
  },
  "keywords": [
    "playwright",
    "scraping",
    "automation",
    "turo",
    "ezpass",
    "toll-automation"
  ],
  "author": "Turo EZPass Automation Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
