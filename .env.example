# AWS Credentials for Turo EZPass Infrastructure
# Copy this file to .env and fill in your actual AWS credentials
# Never commit the .env file to Git!

# AWS Access Credentials
AWS_ACCESS_KEY_ID=<YOUR_AWS_ACCESS_KEY_ID>
AWS_SECRET_ACCESS_KEY=<YOUR_AWS_SECRET_ACCESS_KEY>
AWS_DEFAULT_REGION=us-east-1

# Optional: AWS Session Token (if using temporary credentials)
# AWS_SESSION_TOKEN=<YOUR_SESSION_TOKEN>

# Optional: AWS Profile (if using AWS CLI profiles)
# AWS_PROFILE=<YOUR_PROFILE_NAME>

# Project Configuration
PROJECT_NAME=turo-ezpass
ENVIRONMENT=prod

# Terraform Backend (if using remote state)
# TF_VAR_backend_bucket=<YOUR_TERRAFORM_STATE_BUCKET>
# TF_VAR_backend_key=terraform.tfstate
# TF_VAR_backend_region=us-east-1

# Database Configuration Override (optional)
# TF_VAR_db_instance_class=db.t3.micro
# TF_VAR_db_allocated_storage=20

# Usage Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual AWS credentials in .env
# 3. Load environment: source .env
# 4. Run Terraform: terraform plan && terraform apply